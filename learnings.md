# learnings.md — 会话教训滚动记录（上限 10 条）

## 工作流

- [2026-08-22] 症状：実機「卡死」诊断多次绕弯（OCR+grep 推理 3 轮），用户质问「为什么不用 vision」；规则：**停滞/新页面必派 vision 先行**（页面结构/hitbox/遮挡一眼定案——#34 毛玻璃/#35 hitbox 一次定位），OCR 只补文字；此流程已実証省 2-3 轮诊断
- [2026-08-22] 症状：管线 timeout 收紧 40s 反而全断（尾部锚饿死）；根因：未量「轮询一圈耗时」（32 节点≈60s）；规则：**收紧 next timeout 前先量一圈实际耗时**，timeout≥一圈+15s；提速靠模板化（0.3s vs OCR 2s）而非砍 timeout
- [2026-08-23] 症状：B1 buff 三模板同批老化（好調 0.25-0.36 全失效，三模板裁自同一张旧截图）；规则：**多模板同源素材=同批失效**——一个模板量化失效时先查同源模板（concentration 曾单独重裁的先例即信号）；模板维护走「JSONL known 行选帧→crop v2→reco 互配矩阵（行在≥0.94/误配≤0.81）→同名覆盖→実機 tpl_scores 验证」闭环

## 踩坑

- [2026-08-20] 症状：adb/IPC click posted:true 但界面无变化（本戦 tab/終了/次へ反复）；根因：①pill 按钮 hitbox 内缩——OCR 文字 box 中心偏上 ~21px，几何中心才稳 ②入场动画窗口 agent 点击静默丢失；规则：视觉按钮点击一律用**几何中心**（文字 box 只做存在判定）+Custom 入场 SETTLE 2s；agent 侧点击全走 _tap 守卫链；**vision 给的按钮坐标同样不可信**（2026-08-23 メモリー変換页全選択 vision (100,1060) vs OCR 実測 (82,1001) 偏 60px——按钮定位以 OCR box 中心为准）
- [2026-08-22] 症状：四类毛玻璃面板（卡详情/メモリーアビリティ/buff一覧/P item 详情）挡全画面致锚全 miss→段循环空退；规则：**遮挡类浮层必须在路由根前部有管线层关闭节点**（pos0-2 关闭组），Custom 内 dissolve 只在其宿主可达时有效——「Custom 够不着」是浮层处理的边界
- [2026-08-22] 症状：EventFlag 模板 6日→3日失效+報酬同构子页停滞+変卡扫描只读一排即断底（用户抓出）；根因：模板含变化内容/序列页缺共有锚/单点探针判底；规则：**模板只裁固定元素**（正例跨状态双验证）；同构序列页用「序列推进 Custom」循环点到尽；滚动枚举判底必须**整屏确认**（全部∈已见才到底）；灰卡在识别层过滤（hand 层）优于执行层兜底
- [2026-08-23] 症状：#65 day 读取「3日」在 all_results(score 0.914) 却 filtered 空→误 stop；根因：**expected 含日文字符经 AgentClient IPC 偶发 GBK 乱码**（进程级，5.11/5.12 均有；U+2026/U+FF65 不可编码字符概率最高）；规则：expected 含非 ASCII 且消费 `.hit`/`best_result` = 雷——一律 `[".*"]` 全量+Python 侧子串/正则（省 N-1 次 OCR），四批清零实证
- [2026-08-23] 症状：P 饮料槽四轮 148/148 全 null（実機槽里有瓶）；根因：探测锚窄带全 miss→**全 null 结果被无条件缓存**→`if cached:` 把毒缓存当有效复用整局→available 恒空→「无 P ドリンク」skip 误判前提；规则：**探测失败结果不写缓存**（或写失败标记）+缓存命中校验有效性（至少一槽非空）+attempts 上限防反复探测
- [2026-08-23] 症状：#66 R2 対局被 Round1Flag 误接管（round1 的 9 回合参数跑 R2，轮 11/14 両发）；根因：R2 残り≤9 一位数窗口两位数锚 miss；驱动纪律只防段重启**防不了段内回环**；且 return False 交回路由会被同一 Flag 再接管回环；规则：Custom action 内发现上下文错配时**就地改参重入**（不 return False）；跨大包状态放**全局 session 层**（round1 子树标记会被下一大包的 reset 清掉，活不到消费时点）
- [2026-08-23] 症状：`maa_dev.py test-node ProduceHomeFlag` 连测「纯识别」却把游戏推进到初流程準備页；根因：test-node 用 `tasker.post_task(节点)` 跑的是**完整任务链**（recognition+action+next）；规则：路由节点连测必须断 next 链（已修：test-node 默认 override `{"next": []}`，`--follow-chain` 显式放开）
- [2026-08-24] 症状：vision 读「×タイトルヘ」报「タイトルへ」但 OCR 锚不命中；根因：**假名形/平形（ヘ/へ）视觉不可区分**，vision 输出的形不可信；规则：锚词含假名形/平形同形对时用字符类 `[へヘ]`，或避开该词选更稳锚（#23 标题页取证实践：プロデューサーID 0.98+ 优于 TAP TO START 闪烁文本）
- [2026-08-24] 症状：対局页用前后帧像素 diff 判断「弹出面板是否出现」误报（diff 恒非零）；根因：対局页立绘**持续动画**（待机呼吸/特效），背景像素永远在变；规则：対局页弹面板判定禁用像素 diff——用 OCR 锚（面板专有词）或模板（面板边框）判，像素 diff 只用于静态页（結果页/商店网格）
- [2026-08-24] 症状：跨 0:00 的実機局决策日志「丢失」（回放工具找不到后半局记录）；根因：session JSONL 文件名含日期（`session-YYYYMMDD.jsonl`），**0 点切文件**——后半局写进了新日期文件；规则：长局回放/统计按日期**目录级**收集所有 session 文件，禁止假设单文件含整局

## 工具链

- [2026-08-22] 段驱动数据统计两坑：①决策 JSONL 的 `ts` 是纯 `HH:MM:SS`，与日期字符串比较恒 False/恒 True——时间窗过滤必须用 `ts[:5]`；②灰卡 sat 日志写 `agent-subprocess.log`，该文件按段 "w" 重写——长周期分析**以决策 JSONL（持久追加）为主源**，agent 日志只看当前段；规则：统计过滤先打印首末条记录验证字段格式，结果与观察矛盾先怀疑过滤器
