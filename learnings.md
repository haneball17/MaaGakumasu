# learnings.md — 会话教训滚动记录（上限 10 条）

## 工作流

- [2026-08-21] 症状：汇报「读取层完成」但 buff/P 饮料/P item 枚举全没实现，用户两次抓出（「为什么都没实现」「buff 状态 p 饮料你都没读取」）；根因：把「第一批完成」汇报成「设计完成」，批次边界没明示；规则：完成度汇报必须对照设计清单逐项标注（实现/验证/未动），分批推进时明说「本批不含什么」
- [2026-08-21] 症状：新读取件未実機验证直接 PlayFlag 集成连测两败（跨类常量/面板时序），修完又直接重跑被用户叫停；根因：跳过单元验证省的那 10 分钟换来两次启动周期+排查；规则：识别新件先 probe 直连单元验证（多轮+修复清零重计）全绿才接管线——probe2/3/4 三次实践成型
- [2026-08-21] 症状：识别方案设计的四个关键 UI 约束（P item 随养成增长/饮料格数 3-4 由亲密度/拿不满/省略号溢出+面板内还要滚动）全部来自用户主动告知，实测前我全然不知；规则：设计対局页识别方案前先向用户要已知 UI 约束清单（数量上下限/位置稳定性/溢出行为/形态变体），用户游戏知识是最高优先级事实源
- [2026-08-21] 症状：読数与预期矛盾时反复推理机制模型（好調衰减 vs 回合推进）；根因：动画窗口读数（flow +10000% 峰值）与低置信误读（体力 29→0）混入+小号数字被周边图形干扰（ROI 左界含刻度时 9→a 0.37）；规则：画面数值以「稳定后截图+紧裁 ROI+放大复核」为准，动画期读数只做事件信号；矛盾先怀疑读数再推翻机制

## 踩坑

- [2026-08-20] 症状：adb click posted:true 但界面无变化；根因：adb 点击偶发静默失效；规则：关键点击后必须用 OCR 锚/画面变化验证，无效先原坐标重试 1-2 次再怀疑坐标
- [2026-08-21] 症状：対战页 UI 浮动四连坑——弹窗标题/瓶名随内容浮动（初星黒酢 y722/ビタミン y838）、面板入口 y 漂移、关闭验证锚词与背景状态带同词（ターン内）误报、swipe 起点落面板可交互条目被消费致滚动时灵时不灵；规则：対局页 UI 一律动态锚定（先 OCR 锚定位再取锚相对带，如瓶名=标题 y+90）；关闭验证锚必须选背景不出现的词；面板滚动用容器右缘起点 (545,640)；入口坐标从当次枚举动态取
- [2026-08-21] 症状：滚动清单同行两跑读出长音符变体（ターン内/タン内）致 count 分裂+自一致性误报；根因：日文长音 ー OCR 不稳定；规则：清单类条目键归一化（去 ー）再合并；自一致性判定同理归一比对，残余差异 ≤1 条属单次漏检（双跑并集可达全量）
- [2026-08-21] 症状：probe 复刻 Play 方法逻辑两度与真实行为漂移（audit_details 三 ROI 错连锁/probe4 打开验证不足）；根因：复刻即复制，维护两份逻辑必然漂移；规则：复刻版只作首验探路，最终验证以「手动实证流程固化版」为准，复杂复刻弃用改简化对齐

## 工具链

- [2026-08-20] 症状：maa_dev crop 连续 6 次裁剪只剩 2 个文件、manifest 条目错乱；根因：输出按秒级时间戳命名，同秒互相覆盖；规则：批量裁剪串行执行且间隔 ≥1.3s；改名后须同步 manifest（dict-by-stem 结构）
- [2026-08-21] 症状：MaaMCP run_pipeline 连续调用各带 Custom action，第二次返回 succeeded 但 action 零执行；根因：MCP 自管 agent 子进程拉起/销毁竞态；规则：実機 Custom action 连测一律走 `tools/hif_ipc_runner.py`（AgentClient 直连，bind→connect→register_sink 顺序）；MCP 另有数组参数展平/JSONC 拒载/child_exec 劫持三坑（绕法见 AGENTS.md 开发环境节）
