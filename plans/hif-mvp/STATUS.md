# HIF MVP 当前状态

_快照时间：2026-07-20；当前阶段：M2 Round1 8+2（进行中，安全停点）_

> 2026-07-20 的只读状态观测在控制器连接 `127.0.0.1:16416` 前失败；未产生任何游戏输入。详见 `HANDOFF.md`。当前实机状态必须在 ADB 恢复后重新采证，历史停点不能用于继续出牌。

> 同日已重新连接并完成 Day1 场景3候选枚举。三张候选均可识别但不唯一，已按安全停点停止；来源牌库快照、底部判据、回页验证和结果页证据仍缺失。

## 仓库状态

- 当前分支：`codex/hif-mvp-integration`。
- 创建基点：`fd4d72d docs: 拆分 HIF 决策算法计划`。
- 决策接口与审计规格已提交为 `6f318dd docs(hif): 冻结决策接口与审计规格`。
- 当前 HIF 实机实现、测试、数据、必要模板与文档已提交为 `166b62c feat(hif): 整理实机验证基线`。
- 另有 `.worktrees/hif-round1`，基于 `7c5a764` 分叉，包含一套旧的未提交 simulator/domain 重构；只读参考。
- 大量无关未跟踪目录与文件属于既有工作区状态，不得纳入本任务提交。

## 已完成

- 并行读取并总结两个历史 Codex 会话：
    - `019f639d-bcd4-7780-a416-1d61a1490efe`：决策算法与日志/接口规格，未实现新算法；
    - `019f6542-d40f-77a3-86f3-8df858d869b7`：Round1 指标读取与实机探针，未完成全流程。
- 逐项确认 MVP 范围、两阶段实测、资源边界、成功门槛、状态真值、评分器、`8+2`、并发所有权和版本控制策略。
- 创建集成分支和本任务集中式文档目录。
- 已完整读取并登记 `pipeline-guide`、`pipeline-generate`、`pipeline-option`、`pipeline-testing`；后续 Pipeline
  文档、节点、选项和测试必须按对应技能执行。
- 完成 M0 权威基线审查与分拆提交；未纳入 `debug/`、视频、模型、测试资源和无关未跟踪目录。
- 路线评分器子提交已审查并择取为 `482764e feat(hif): 新增当前路线确定性评分器`；当前可信停点唯一选择 `祝福+`，缺失、过期、未知、灰卡和并列均类型化拒绝。
- 完成 Pipeline 首轮只读审计，证据化缺口见 `audits/pipeline-m1.md`；已以 `09936af` 移除 HIF 根路由的识别阶段隐藏输入。
- 状态读取子提交已审查并择取为 `b155c52 feat(hif): 严格读取并对账局内状态`；后续实测撤销了“右上 22 是牌库”的错误语义，确认其为元气，牌库详情页真值为 `21`。
- 普通 `ProduceCardsHIF` 已接入路线状态组装与确定性评分器：只要求实际依赖字段，多 ROI 交叉一致，当前完整状态唯一绑定 `祝福+`；缺失、冲突、灰卡、未知卡和并列均安全拒绝。
- M1 可信绑定运行 `hif-mvp-m1-consensus-bind-stop-20260715` 仅有四次只读面板输入，没有手牌点击，并按预期在未实现语义后验时停止。
- 当前环境可用技能中缺少 `pipeline-testing`；后续以仓库的节点级测试、`hif_pipeline_check.py`、全 bundle 正式集成测试和控制器日志审计达到同等证据门槛，并持续记录该限制。

## M0 验证基线

- HIF 专项：`284 passed`。
- 全仓：`337 passed`。
- `python tools/hif_pipeline_check.py`：`ok=true, issue_count=0`。
- Python 编译：通过。
- `python -m ruff check ...`：通过，仅有 `pyproject.toml` 顶层配置弃用警告。
- 本次触及且采用仓库 Prettier 风格的 JSON/Pipeline 文件检查通过；全量 `assets/data/hif/**/*.json` 检查仍会命中 11 个历史格式不一致文件，其中多数未被本任务修改。为避免无关全文件重排，M0 不自动格式化这些历史数据文件。
- `git diff --check`：通过。

## 当前实机停点

最近可信证据来自：

- 运行目录：`debug/hif-live/hif-round1-score-multiplier-calibrated-20260715/`
- Journal：`debug/hif-journal/20260715T185253-45208.jsonl`

已知状态：

- 页面：Round1；剩余回合 `6`。
- `good_condition=47`、`focus=10`、`stamina=33`、`reprise=2`。
- `current_score=116611`。
- `stage_multiplier=3807%`。
- 可见手牌：`演出計画`、灰色 `眠気`、`祝福`、`始まりの合図`；最近零输入帧见 `debug/hif-live/hif-mvp-m2-zero-input-recheck-20260716.png`。
- 当前牌库数量尚未可靠重读。
- 指标面板关闭后已验证回到 Round1；本次规划与建档没有执行新的实机输入。

最新安全停点来自 `debug/hif-live/20260716T091431/after.png`：

- Round1、剩余回合 `6`、分数 `137665`、倍率 `3807%`、体力 `29`、好调 `50`、集中 `10`。
- 手牌为 `演出計画`、灰色 `眠気`、`始まりの合図`。
- 运行 `20260716T091431` 错误地将路线评分的「祝福」排序结果作为执行授权，因而完成了一次「祝福」确认出牌；其后验以 `post_state_not_readable` 拒绝，不计入严格成功样本。该授权漏洞现已关闭：`hand_details_map_deck_play_one` 在任何输入前安全停止。

## 当前主要缺口

- 基线差异尚未按范围审查和提交。
- 普通决策链尚未稳定消费分数、倍率和所有动作必需状态。
- 牌库数量、卡牌详情和重启后账本对账仍需完成。
- 当前路线结构化卡牌效果与确定性评分器尚未完成。
- Round1 尚未达到 `8+2`；Round2 尚未开始独立采证。
- `祝福+` 已形成模型发现样本但不计严格成功：动作后基础卡面值受活跃状态联动，且旧固定 `reprise` ROI 会随状态列表位移读到其他计数。下一步必须动态定位状态图标并建模相关减耗、好调与得分触发。
- 状态详情探针已随 `a01d5d4` 落地；实机确认了参数上升量 `30%/1ターン`、体力消耗 `50%轻减/5ターン`、绝好调 `2ターン` 与发动预约。仍需将这些动态状态接入严格后验。
- 活跃效果读取已在实机 Journal `20260716T002051-49616.jsonl` 验证为 `execution_ready=true`；OCR 结果按文本框坐标恢复阅读顺序，歧义值仍拒绝。下一步是把该快照纳入卡牌后验，而不是解除通用出牌门禁。
- 路线评分器只能提供排序，不可直接授权控制器输入；每张待执行卡都必须有独立、稳定且可读取的后验契约。
- Interval、Round2 后续、奖励、Live、回忆和真实优胜结算仍缺完整闭环。
- 决策核心只有冻结规格，尚未实现。
- 正式 Round1/Round2 仍是观察/单步停止模式；Interval、Live、结算、回忆、随机奖励链和通信错误恢复尚未形成可验收主链。
- Round1 通用 `残りターン` 识别可能抢占 Round2；必须取得 Round2 当前实帧后建立互斥识别。

## M2 下一步

1. 从最新三卡停点重新采集同运行期状态与活跃效果，并为候选卡定义分数、好调、体力、手牌与牌库的精确动作后语义断言，先写失败测试。
2. 只有逐卡后验获得显式授权时才完成两段式同目标确认；失败立即停在可重建状态，路线排序本身不得触发点击。
3. 继续补足 Round1 剩余成功样本；拒绝样本至少保留灰卡和状态缺失两种不同原因的零危险输入证据。
4. 达到独立 `8+2` 后才开放 Round1 有界循环，并继续每步重读、重评分、目标绑定和语义后验。

## 阻塞与风险

- 当前两个 worktree 的旧实现高度重叠，禁止整体合并。
- 实机是单一可变状态，只允许主整合 agent 串行输入。
- 当前 Round1 现场不可替代，任何动作前必须重新截图并核对停点。
- 子 agent 模型由平台分配，不能保证指定模型或 reasoning effort；通过窄任务、证据要求和主 agent 审查控制质量。
