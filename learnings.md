# learnings.md — 会话教训滚动记录（上限 10 条）

## 工作流

- [2026-08-21] 症状：汇报「读取层完成」但枚举全没实现，用户两次抓出；规则：完成度汇报必须对照设计清单逐项标注（实现/验证/未动），分批推进时明说「本批不含什么」
- [2026-08-21] 症状：新读取件未実機验证直接集成连测两败；规则：识别新件先 probe 直连单元验证（多轮+修复清零重计）全绿才接管线；复刻版只作首验探路（两份逻辑必漂移）
- [2026-08-21] 症状：识别方案的关键 UI 约束（P item 随养成增长/饮料格数随亲密度/灰卡变灰等）全部来自用户主动告知；规则：设计対局页识别方案前先向用户要已知 UI 约束清单，用户游戏知识是最高优先级事实源（2026-08-22 追証：灰卡约束同模式）
- [2026-08-22] 症状：実機「卡死」诊断多次绕弯（OCR+grep 推理 3 轮），用户质问「为什么不用 vision」；规则：**停滞/新页面必派 vision 先行**（页面结构/hitbox/遮挡一眼定案——#34 毛玻璃/#35 hitbox 一次定位），OCR 只补文字；此流程已実証省 2-3 轮诊断
- [2026-08-22] 症状：管线 timeout 收紧 40s 反而全断（尾部锚饿死）；根因：未量「轮询一圈耗时」（32 节点≈60s）；规则：**收紧 next timeout 前先量一圈实际耗时**，timeout≥一圈+15s；提速靠模板化（0.3s vs OCR 2s）而非砍 timeout

## 踩坑

- [2026-08-20] 症状：adb/IPC click posted:true 但界面无变化（本戦 tab/終了/次へ反复）；根因：①pill 按钮 hitbox 内缩——OCR 文字 box 中心偏上 ~21px，几何中心才稳 ②入场动画窗口 agent 点击静默丢失；规则：视觉按钮点击一律用**几何中心**（文字 box 只做存在判定）+Custom 入场 SETTLE 2s；agent 侧点击全走 _tap 守卫链（坐标+结果截图+指纹变化留痕）
- [2026-08-22] 症状：四类毛玻璃面板（卡详情/メモリーアビリティ/buff一覧/P item 详情）挡全画面致锚全 miss→段循环空退；规则：**遮挡类浮层必须在路由根前部有管线层关闭节点**（pos0-2 关闭组），Custom 内 dissolve 只在其宿主可达时有效——「Custom 够不着」是浮层处理的边界
- [2026-08-22] 症状：EventFlag 模板 6日→3日失效+報酬同构子页停滞+変卡扫描只读一排即断底（用户抓出）；根因：模板含变化内容/序列页缺共有锚/单点探针判底；规则：**模板只裁固定元素**（正例跨状态双验证）；同构序列页用「序列推进 Custom」循环点到尽；滚动枚举判底必须**整屏确认**（全部∈已见才到底）；灰卡在识别层过滤（hand 层）优于执行层兜底
- [2026-08-22] 症状：ClassOptionFlag 在変卡页误命中→no_safe_option stop（轮 7 #48）；根因：変卡页与授業页**共享左上「授業」HUD**（両页顶部 OCR 实证逐项一致）——锚位+文字都一致，收窄无解；规则：跨页共显锚不可收窄时走 **action 侧自检他页专有锚放行**（OCR 変卡「チェンジ」命中即 return True 交回路由），放行必带连续 N 次上限——[JumpBack] 回环命中即重置轮询永不到 timeout，无限放行=无限空转；段边界 turn/数值读空同理：**指纹变化=动画中等待重读**（`_wait_turn_reframe` 模式）而非立即 stop

## 工具链

- [2026-08-22] 段驱动数据统计两坑：①决策 JSONL 的 `ts` 是纯 `HH:MM:SS`，与日期字符串比较恒 False/恒 True——时间窗过滤必须用 `ts[:5]`（実機轮 7 统计得 0 条假结果，与监控观察矛盾才追出）；②灰卡 sat 日志写 `agent-subprocess.log`，该文件按段 "w" 重写——长周期校准数据必须落独立 JSONL/决策 evidence 字段，禁落随段滚动的日志。规则：**统计过滤先打印首末条记录验证字段格式**，结果与观察矛盾先怀疑过滤器
