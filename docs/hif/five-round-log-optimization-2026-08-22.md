# 五轮执行日志复盘优化清单（2026-08-22）

对五轮全程验证（`five-round-validation-2026-08-21.md`）的执行日志做统计挖掘后的优化裁决记录。数据源：`debug/decisions/session-20260822.jsonl`（546 行）、`ops-20260822.jsonl`（1850 行）、`debug/custom/2026-08-22.log`、`debug/autodev/round*_loop*/`（57 个日志、469 个 SEG 段）、`stall-*.json`（14 个）。

## 一、复盘画像

### 总览

- 全程墙钟 ~15.2h（00:26→15:39），纯游戏段 ~6.3h；稳定态 R3/R4 ~75min、R5 ~85min。
- 出牌回合间隔中位 42s/步（p90 124s，n=299）；R2 12T 约 10 分钟。
- 路由圈 32 节点 ≈60s/圈（OCR ~2s/节点）。

### 已确认修复生效（无需再动）

| 信号                           | 修复前         | 修复后               | 依据                           |
| ------------------------------ | -------------- | -------------------- | ------------------------------ |
| `turn_counter_unreadable` stop | R1/R2 共 59 次 | R3-R5 降至 1-3 次/轮 | #24 crop-zoom + 回合数上限校验 |
| `scroll_back_failed`           | R1 24 次       | R2 起归零            | 牌堆 scroll 探测路径           |

### 仍活跃问题（R4/R5 仍发生，按频次排）

| #   | 问题                                                  | 证据                                                             | 影响                                              | 处置                 |
| --- | ----------------------------------------------------- | ---------------------------------------------------------------- | ------------------------------------------------- | -------------------- |
| 1   | 好調行模板定行失败 → `good_condition_turns` 兜底 None | 156 次，R5 尾部仍 21 次/半小时                                   | 策略输入质量（好调系核心参数）                    | backlog B1           |
| 2   | 开局探测降级（P item 弹窗锚 miss + 牌堆假设位 miss）  | 106 次，R4/R5 共 ~42 次                                          | 每段浪费 10-30s；策略不消费此二字段（审计缺口 4） | **已修**：开关默认关 |
| 3   | 段停滞需人工恢复                                      | 14 次（结算/主页 5、差し入れ弹窗 4、再挑戦確認 3、技能卡详情 2） | 全部人工 snap+vision+重启段                       | backlog B2           |
| 4   | turn_score None 12% / total_score None 10%            | 231 条含 evidence 记录                                           | replay 报告质量、exec_verified score 分量失效     | **已修**：crop-zoom  |
| 5   | 灰卡 sat 日志 debug 级采不到（0 条）                  | `_is_gray_card` 用 debug 级                                      | 遗留#5 阈值校准无数据源                           | **已修**：判灰 info  |
| 6   | ops `scene_changed` 1850/1850 全 true                 | 无一 false                                                       | 作为「点击生效」信号无区分度                      | backlog B4           |

## 二、本日裁决（grill 2026-08-22，全按推荐）

- **Q1 产出形态**：落盘本清单 + 本会话修低风险 3 件（Q3/Q4/Q5）；性能大项另开会话。
- **Q3 开局探测**：`probe_opening_resources` 开关默认关（preset 字段 + GUI/override 两侧解析）；実機取证校准坐标后（遗留#2/#3）再打开。
- **Q4 score 读空**：`_read_total_score` 直读 miss → ROI crop 放大 3 倍重读（`_read_turn_zoomed` 同模式），提取 `_max_int_from_ocr` 复用。
- **Q5 灰卡日志**：判灰升级 info 带 sat 值（低频不刷屏），非灰保持 debug。
- **Q2 好調模板**：进 backlog（修法倾向重裁模板+多尺度，需実機画面有好調 buff 时截图取证，离线做不了）。
- 停滞映射表 / scene_changed 改造：进 backlog（见 B2/B4）。

## 三、Backlog（按建议优先序）

- **B1 好調模板重裁 + 多尺度匹配**：実機截好調/絶好調 buff 行清晰图标重裁 TPL_GOOD（当前 156 次失败），probe 验证 0.85 阈值下自匹配/互配区分度；R5 尾部仍高频，是策略输入质量第一缺口。
- **B2 停滞签名→自愈动作映射表**：14 次人工恢复的停滞签名全部可自动化（结算/主页→判定段完成、差し入れ弹窗→GiftTalk 推进、再挑戦確認→RetryConfirm 点終了、技能卡详情→关闭）；把 segment_loop 的 stall 签名检测接到已有节点/动作，是「主线零介入」扩到「全程零介入」的直接路径；需段驱动実機联调。
- **B3 OCR→模板 33 节点改造续作**（已批准，仅完成首件 hif_next_button.png）：路由圈 60s→模板后可大幅缩短；收紧任何 timeout 前先量一圈实际耗时（既有铁律）。
- **B4 scene_changed 区分度改造**：exec_verified（2026-08-22 已落地）已覆盖出牌假成功场景；若后续仍需 ops 级点击生效信号，改 ROI 限定指纹（点击目标周边）或降采样帧差阈值，P2 再议。
- 遗留 7 项（入口链自动化、P item 坐标、牌堆取证、探索分支、灰卡阈值、瓶位语义、開場コミュ）见 `five-round-validation-2026-08-21.md` 尾节；P1 日志项（round_tag 注入、节点流尾部、heal 事件）沿用 handoff 排序。
