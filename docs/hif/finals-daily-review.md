# HIF 本战每日记录：首轮视觉审阅（2026-07-12）

审阅对象为 [finals-daily-log.md](finals-daily-log.md) 引用的本地实机截图。原始截图不作修改；红框审阅副本与每张图的 ROI 编号索引输出到 `debug/hif-daily-review-20260712/`。

## 覆盖结论

- 已逐张视觉审阅日志中当前可读取的 55 张截图（包括收尾阶段的备份图）。
- 日志所有已定义 ROI 小节均已解析，34 个小节均已生成红框标注图和同名 `.txt` ROI 索引。
- 所有已生成的标注图均基于 `720x1280` 原图；红框与日志中的 `[x, y, width, height]` 一一对应。
- 原日志中缺失的 PotPlayer 截图已在 `assets/resource/test/` 找到备份并纳入标注；审阅产物中不存在缺图项。

## 已确认的页面链路

1. 本战前 6 日：三属性授业 → 三事件选项 → 效果预览 → 两阶段 `セレクトチェンジ` → 完成提示。
2. 本战前 5 日：三属性公开 lesson → 预览 → 实际结算；预览收益与结算收益不应视为相同数值。
3. 本战前 4 日：`おでかけ` / `差し入れ` 候选 → P 饮料三选一 → 技能卡三选一/重抽 → 饮料持有上限取舍 → P item 变卡机会。
4. 本战前 3 日：三属性授业 → Vi 三选项 → 好调关系变卡 → 牌库滚动选择 B → 变更确认。
5. 本战前 2 日：公开 lesson 三选一与 P 饮料上限事件。
6. 本战前 1 日：唯一 `相談` 候选 → P 点商店，可安全识别 `終了`。
7. Round1：局内状态、三张初始手牌、饮料栏与底部菜单可见。
8. Interval：商品、牌库、强化、特别指导、回复确认均有连续实测截图。

## ROI 审阅结论

现有 ROI 在首轮视觉复核中与对应 UI 元素对齐，适合作为“候选 ROI / 标注基线”，但还不能直接当作可执行坐标：

- 所有文字与数值 ROI 仍需要 OCR 实测成功率验证。
- 所有卡牌网格、商品区和候选区应使用检测框或唯一候选结果，而不是直接点击固定坐标。
- 有资源消耗的 `再抽選`、`受け取る`、`チェンジ`、`交換する`、`強化`、`カスタマイズ`、`回復する` 应保留前后帧验证。
- `Live` 是横向演出布局，不能直接复用竖屏页面的点击/ROI 假设。

## 收尾阶段补充审阅

以下备份图位于 `assets/resource/test/`，已视觉复核并完成标注：

- Live、分数结算、回忆照片选择、照片确认、回忆生成、回忆卡预览。
- `221442.375` 是 Live 加载帧，`221525.665` 是照片确认的补充帧，作为流程证据保留，不新增日志 ROI。

## 复现标注

当前系统 Python 缺少 Pillow；请使用工作区提供的图像 Python，或任意具备 Pillow 的 Python 环境：

```powershell
& "C:\Users\haneball\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe" tools/hif_daily_roi_review.py --output "debug/hif-daily-review-20260712"
```

输出目录中：

- `01-*.png` 至 `34-*.png`：按日志 ROI 小节生成的红框图。
- 同名 `.txt`：红框编号、元素名称与 ROI 坐标。
- `contact-sheet-*.png`：红框图总览。
- `source-contact-sheet-*.png`：全部现存原始截图总览。
- `README.md`：ROI 小节、来源、产物和缺图状态的完整索引。
