# Plan: 完成现有 HIF 管线自动化测试并修复已证实缺口
_Locked via grill — 由当前 Codex 代理与用户共同确认_

## Goal

为当前已有实机证据的 `rinami_garakuta_road` HIF 路线建立可重复、可审计的自动化测试闭环：以 Pipeline 节点与跳转的行为覆盖矩阵为主线，结合最小真实帧夹具、纯逻辑测试、受控动作模拟和结构校验，补齐已实现页面的成功路径、拒绝/安全停止路径与后验失败路径；测试暴露的实现或路由缺口采用测试驱动的最小修复。未采证页面、其他偶像/卡组路线以及需要新实机输入才能证明的行为继续显式安全停止。本计划不执行模拟器点击，也不承诺完整连续培育或自动出牌。

## Approach

1. 冻结并记录当前基线。
   - 将当前含大量未提交 HIF 改动的工作区视为权威基线，不回退、覆盖或格式化无关文件。
   - 记录当前 HIF 专项基线（现为 `153 passed`）、`hif_pipeline_check.py` 结果及 HIF 相关文件清单。
   - 枚举 `ProduceHIF.json` 的全部节点、入口、跳转、Custom Action/Recognition 注册和跨文件引用；当前审计基线为 54 个节点、34 条通往 `ProduceHIFUnknownStop` 的保护边。

2. 建立机器可检查的 HIF 行为覆盖矩阵。
   - 为每个当前 HIF Pipeline 节点标注类别：入口/路由、识别、已采证动作、只读观察、显式安全停止或外部共用节点。
   - 对已采证且已实现的节点至少登记：成功路径、权限拒绝或安全停止路径、动作后验失败路径；不适用的分支必须写明理由，而不是默认为已覆盖。
   - 将覆盖矩阵做成测试可读取的数据或参数化用例，使新增节点、删除节点或未登记节点直接导致测试失败。
   - 扩展结构检查：节点引用存在、注册名存在、入口可达、关键结果页优先级正确、安全推进不会抢占已知交互页、受保护动作最终可到达 `ProduceHIFUnknownStop`。

3. 建立最小真实帧回归夹具。
   - 仅从已经采证且用户允许使用的本地素材中选择必要的原始 `720x1280` 帧；不提交 `debug/`、Journal、日志、视频或无关大文件。
   - 为每个夹具记录来源说明、截图环境、分辨率、预期页面、应命中的锚点以及必须拒绝的近似页面；确认不包含账号或其他敏感信息。
   - 真实帧用于页面分类、ROI、模板和可离线执行的图像预处理回归；Maa OCR/控制器不可稳定离线执行的部分使用明确的适配器结果夹具，不伪装成真实 OCR 端到端测试。
   - 对易混淆页面加入成对反例，例如奖励选择态/展示态、课程选中态/结果态、变卡目标页/源卡牌库、日程页/安全推进候选页。

4. 分层补齐自动化测试。
   - 结构层：Pipeline 图、Custom 注册、任务覆盖参数、入口和路由顺序。
   - 识别层：真实帧与屏幕配置的正例、反例、ROI 边界、全角/半角及已知受限 OCR 容错。
   - 决策层：日程、课程、饮料、技能卡、变卡、咨询、Interval 与 Round 观察策略的确定性输入输出；只保证当前 `rinami_garakuta_road` 数据。
   - 动作层：使用假控制器、假截图序列和可控时钟验证两段式选择、候选枚举、授权门、唯一目标、点击前置条件与动作后后验。
   - 安全层：未知页面、OCR 为空、候选不唯一、权限缺失、按钮不唯一、帧变化但目标页未命中、运行状态丢失时必须在发送危险输入前停止，或在已授权动作后立即安全停止。
   - 运行器层：实验开关互斥、默认无资源操作权限、从主界面启动时不覆盖正式根路由、证据只能写入 `debug/`。

5. 按已采证页面逐项审计并补齐测试。
   - 入口与准备：HIF 模式选择、最终开始确认、正式根路由恢复。
   - 日程与课程：日程 OCR/颜色识别、两段式确认、课程预览锚点、课程结算过渡、公开课结果。
   - 奖励：奖励袋、P 饮料候选与展示层、技能卡候选与展示层、满仓保留状态。
   - 变卡：目标候选与重抽耗尽、目标状态传递、源卡只读枚举/显式确认、完成提示及受限 OCR 容错。
   - 其他已存在节点：咨询只读/受限结束、Interval 的支持模式与拒绝模式、结算继续、Live 观察、回忆照片步骤、Round1 只读观察。
   - 对尚无实机证据的分支只验证明确的“不支持原因 + 安全停止”，不新增猜测性自动操作。

6. 测试先行修复真实缺口。
   - 每个缺口先以最小失败用例重现，再修改实现；修复后同时保留成功与负向回归。
   - 允许仅为测试与职责隔离而提取纯识别、解析、决策或后验辅助函数；设备 I/O 与 Maa Action 保留在现有入口层。
   - 保持 Pipeline 节点名、Custom Action/Recognition 名称、运行器参数、任务配置和已有数据格式向后兼容。
   - 禁止借机重写 HIF 架构、扩大 ROI、放宽未知 OCR、以“帧发生变化”代替目标后验，或给默认模式新增资源操作权限。

7. 完成文档化与一致性审计。
   - 更新 HIF 测试覆盖说明与 `docs/hif/handoff.md`，记录已覆盖节点、仍保留的安全停止及无法离线证明的实机缺口。
   - 确认未把 `debug/`、Journal、运行日志、视频或无关资源纳入改动。
   - 审阅最终差异，区分本轮改动与工作区既有改动，不回退用户内容。

8. 通过双层验收门禁。
   - HIF 专项测试必须全部通过。
   - 运行全仓 `pytest`，检查共享代码回归；若出现可证明与 HIF 无关的既有失败，保留完整证据但不扩大修复范围。
   - 运行 `python tools/hif_pipeline_check.py`、相关 Python 编译、Ruff，以及对本轮改动 JSON 的 Prettier/格式检查。
   - 不进行新的模拟器点击。可选的设备检查只能截图或读取状态；任何需要推进游戏状态的验证列入后续独立实机任务。

## Key decisions & tradeoffs

- “完成”指覆盖当前已实现、已有实机证据的页面；未采证行为以可解释的安全停止作为正确结果，不等同于完整 HIF 连续自动化。
- 使用节点/跳转行为覆盖矩阵作为完成度门槛，不设置缺乏语义的单一行覆盖率百分比。
- 允许少量真实帧进入版本化测试夹具，以捕获 ROI、模板和页面分类回归；通过最小化、来源记录和敏感信息检查控制仓库体积与资源风险。
- 允许测试驱动的最小模块拆分，但保持所有外部 Pipeline 与 Action 契约兼容；不进行整体架构重写。
- 本轮仅支持 `rinami_garakuta_road`；其他路线必须明确拒绝或安全停止。
- 本轮不执行新的实机点击，因此部分动画、真实 OCR 时序和点击坐标只能由已有证据约束，不能被宣称为新完成的实机闭环。
- 最终同时运行 HIF 专项与全仓检查；只修复与 HIF 改动有因果关系的失败。

## Risks / open questions

- 当前工作区已有约 5000 行 HIF 相关未提交改动，且计划会触及相同文件；实施时必须逐块审阅差异，避免覆盖用户或前序会话内容。
- 现有测试大量依赖 mocked OCR 与模拟帧；真实帧夹具能覆盖 ROI/分类，但无法完全复现 Maa OCR、控制器和动画时序。此限制必须在测试名称与文档中明确。
- 本地未跟踪素材包含压缩包、视频和多张截图；实施时只能挑选计划所需的最小帧，来源不明或含敏感信息的素材不得纳入。
- 某些动作由游戏随机候选或跨节点会话状态驱动。测试需要固定候选、截图序列与时钟，避免把随机性和等待时间带入 CI。
- 若审计发现某个“已支持”节点缺少足够实机证据，默认将其降级为显式安全停止并记录，而不是依据代码意图补出自动点击。

## Out of scope

- 新增其他偶像、歌曲、卡组或 HIF 策略配置。
- 完整连续 HIF 培育、Round 自动出牌或完整培育实机闭环。
- 任何新的模拟器点击、资源领取、变卡、出牌或游戏进度消耗。
- 为未采证页面猜测按钮、ROI、OCR 文案、动画时序或候选策略。
- 同步 `origin/main`、解决分支合并冲突、发布、打包或更新版本号。
- 提交 `debug/`、Journal、日志、视频、压缩包或无关游戏资源。
- HIF 之外的功能修复，以及无关代码的大规模格式化或重构。
