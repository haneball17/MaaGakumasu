# Plan: 当前 HIF 实机全自动闭环测试

_Locked via grilling — 由用户与当前 Codex 代理于 2026-07-14 逐项确认_

## Goal

从当前 MuMu 12 中尚未领取的 HIF 技能卡奖励页继续，针对已有实机证据的
`rinami_garakuta_road` 路线，边采证、边补齐实现与自动化测试，安全推进当前这一局 HIF 至最终结算。

本计划的目标不是简单“把页面点过去”，而是建立可重复、可审计的实机闭环：每个资源操作和 Round
出牌都必须有唯一目标、明确前置页面、受控输入、动作后目标后验和 Journal/截图证据；发现实现缺口时先以
失败测试固定，再做最小修复并继续当前局。除硬性停止条件外，执行期间不再逐页请求用户确认。

## Frozen authorization

用户已明确授权本计划在**当前这一局 HIF 培育**内自主执行以下操作：

- 点击和切换候选、技能卡领取、必要时使用剩余免费重抽、日程/事件选择、Round 出牌及页面推进；
- 消耗当前局内已有的体力、P 点、P 道具、P 饮料和其他局内资源；
- 进行只读截图、OCR/YOLO 采证、Journal 审计、日志检查、代码修改、离线测试和测试工具重启；
- 通信错误、游戏崩溃或返回标题页时，自动重启游戏，并通过
  `Tap to Start -> プロデュース再開` 恢复**同一局**；
- 失败或可恢复异常时使用游戏提供的免费重试/继续机会。

完整授权不取消安全门，且严格排除：

- 有偿宝石、现实货币、礼包、充值或其他购买；
- 登录凭据、账号切换、账号设置及游戏外账户操作；
- 主动放弃当前局、开启下一局或把授权扩展到其他路线；
- 猜测按钮、坐标、OCR 文案、候选策略或动画时序；
- 仅以“画面发生变化”作为资源操作或出牌成功的后验；
- `git push`、发布、打包、同步主线、提交日志/视频/压缩包或清理用户文件。

只有触及上述排除项、必须放弃/重开当前局、需要新增账户级权限，或无法安全恢复时，才重新请求用户介入。

## Starting point

- 仓库：`D:\code\MaaGakumasu`
- 分支：`feat/hif`
- 基线 HEAD：`8c5471b feat(hif): 完善安全自动化测试闭环`
- 当前工作区存在 HIF 实测实现、测试、数据与文档的未提交差异，以及大量无关未跟踪目录/文件；必须保持原样，不得用旧基线描述覆盖当前状态。
- 命令输出必须受限；优先使用 `rtk` 包装的 PowerShell/`rg` 与项目命令。
- 模拟器：MuMu 12，实例 `1`，ADB `127.0.0.1:16416`。
- ADB：`D:\game\MuMuPlayer-12.0\nx_main\adb.exe`。
- 原始帧契约：竖屏 `720x1280`。
- 技能卡领取已完成且已进入 Round1；所有“技能卡领取页”“三候选”与“领取 `祝福`”描述仅为历史证据，禁止再次执行其入口。
- 最新成功的同进程观察为 `debug/hif-live/hif-round1-five-card-map-deck-retry-20260715-051108/`，Journal 为
  `debug/hif-journal/20260715T051112-5384.jsonl`：完整手牌为 `話題沸騰 / 鳴り止まない拍手 / 夏夜に咲く思い出 / 仕切り直し / 始まりの合図`，`deck_size=21`，且 `missing_after=[]`。
- 最新复合观察 Journal `debug/hif-journal/20260715T052515-16536.jsonl` 的完整状态是
  `turn=6 / good_condition=47 / focus=5 / stamina=33 / reprise=2 / deck_size=21`；精确策略唯一目标为非灰卡 `話題沸騰`，尚未执行。
- 现有 `--round1-play-one` 在新进程中没有详情映射，不得用于当前停点。Round1/2 的正式 Pipeline 仍默认
  `observe_and_stop`，连续模式继续拒绝；下一次输入只能来自新的“映射、决策、执行”同一运行期单卡入口。
- 历史 HIF 门禁的 `245 passed, 45 deselected` 和 Pipeline `ok=true, issue_count=0` 不可代替当前工作区的重新验证；任何实机输入前必须重跑直接相关检查。

## 实机进度（2026-07-15 02:14）

- 当前局已进入 Round1 第 2 回合，未进入下一局。
- 初始手牌经多次 YOLO + 卡名 OCR 稳定识别为 `静かな意志+ / 至高のエンタメ+ / 始まりの合図+`；原始框、标签、置信度、强化后缀和重复框抑制均写入 Journal。
- `debug/hif-journal/20260715T011036-39844.jsonl` 额外保存了游戏内 `手札情報確認` 页面证据，明确显示 3 张初始手牌；对应实机 run 为 `debug/hif-live/hif-round1-deck-probe-20260715-003/`。
- 只读牌库探针已确认 `deck_size=20`，OCR 原文 `スキルカード(20)`，置信度 `0.999133`；正确入口为底部箱包 ROI `[519,1174,80,82]`。
- 首回合执行前完整状态为 `turn=9, flow=Vi, good_condition=6, focus=6, stamina=28, deck_size=20, reprise=0`，其中 `reprise=0` 仅在 `turn == total_turns` 时按机制推导。证据：`debug/hif-journal/20260715T012316-33684.jsonl`。
- 首张唯一目标为 `至高のエンタメ+`。首次点击只进入带精确卡名和 `SELECT` 的选中态；恢复命令随后执行绑定目标的第二次点击。Maa 日志确认恢复 run 仅有一次点击 `(338,982)`。
- 恢复 run `debug/hif-live/hif-round1-confirm-selected-20260715-001/` 的即时手牌采样落在抽牌动画内，因此 Journal 保守记录 `post_hand_not_readable`，没有伪报成功。
- 后续零点击 run `debug/hif-live/hif-round1-post-card-observe-20260715-001/` 证明首牌已生效：`turn 9→8`、`focus 6→4`、`flow Vi→Vo`，且 `至高のエンタメ+` 已离开手牌。新手牌稳定识别出 `自然体の魅力+ / お姉さんの感覚 / 鳴り止まない拍手+`，左侧 `仕切り直し+` 尚未被 OCR 读出。
- `仕切り直し+` 已加入仅识别词典。只读证据 `debug/hif-journal/20260715T014838-48588.jsonl` 完整记录四张手牌的原始框、YOLO 标签/置信度、OCR 原文/置信度：`仕切り直し+ / 自然体の魅力+ / お姉さんの感覚 / 鳴り止まない拍手+`。
- 首牌后恢复规则现严格要求 `round1 / total_turns=9 / turn=8 / 仅 reprise 缺失`，不会覆盖已读出的非零 `reprise`，也不会在其他字段缺失时污染 session。
- `お姉さんの感覚` 已通过绑定标题与 `SELECT` 的两段式确认执行。后验 `debug/hif-journal/20260715T015340-28268.jsonl` 验证目标离开手牌；稳定零点击观察 `debug/hif-journal/20260715T015511-40156.jsonl` 显示 `turn=7, flow=Vi, good_condition=19, focus=4, stamina=30`，但 turn OCR 将画面 7 误读为 1，禁止据此自动继续。
- 2026-07-15 02:03 的首版逐张详情探针存在缺陷：实机读取了不存在的 `reader._card_dict` 并抛异常，MaaFramework 随后重复执行节点。run `debug/hif-live/hif-round1-hand-detail-probe-20260715-001/` 的 `maafw.log` 记录 7 次左侧点击；逐帧证据 `debug/hif-journal/20260715T020310-6812-0003/0006/0009/0012/0015/0018/0021-*.png` 证明实际顺序为：选中并执行 `スポットライト+`、选中并执行 `スリリング+`、选中并执行 `国民的アイドル+`、最后仅选中 `自然体の魅力+`。未结束回合、未进入下一局、无付费操作。
- 事故后已加入三层保护：使用独立卡名字典、探针前拒绝已有 `SELECT`、session 一次性锁与动作内异常转安全停止；后续没有再发生重复点击。
- 当前手牌已按详情页逐张实证为 `自然体の魅力+ / 天賦の才+ / シュプレヒコール++`。证据：`debug/hif-journal/20260715T021109-14704.jsonl` 记录前两张，`debug/hif-journal/20260715T021428-31988.jsonl` 以零控制器输入补录第三张；当前停在 `シュプレヒコール++` 选中态。
- 当前未选中事故帧可确认实际状态为 `turn=7, flow=Vi, good_condition=40, focus=4, stamina=30`；当前选中 `シュプレヒコール++` 后显示的 `good_condition=46 / focus=2` 是出牌预览值，不得写回当前状态。意外执行的三张牌及再演/技能使用次数改变了状态，旧的首牌后 `reprise=0` 恢复规则不得再次使用。
- 用户确认灰色手牌表示当前不可打出；YOLO 的 `useless` 标签现作为执行硬门。灰牌仍保留在“手牌存在”语义和详情证据中，但从首牌默认目标、绑定确认和所有实际出牌目标集合中排除。当前 `自然体の魅力+` 为灰牌，不得执行。
- 回合 OCR 收紧 ROI 后仍空读；零输入多 ROI 探针 `debug/hif-journal/20260715T023353-45248.jsonl` 对 5 组候选 ROI 均返回空文本，说明该样式不能继续依赖普通 OCR，应改为数字模板或专用图像分类。
- 出牌后验已改为手牌不可读时最多 4 次有限等待重读；最新 HIF 门禁为 `245 passed, 45 deselected`，Pipeline `ok=true, issue_count=0`，Ruff 与 `compileall` 通过。
- 下一步：为已采证的 `turn=9/8/7` 建立数字模板识别，并从详情证据和事故帧重建当前 `reprise`、技能卡使用次数与牌库数量。只有完整状态和唯一可打出目标重新成立后，才允许确认当前 `シュプレヒコール++` 或切换目标。

## Authoritative files

新会话先读取，不从旧聊天记录猜状态：

- `AGENTS.md`
- `README.md`
- `docs/zh_cn/功能说明.md`
- `docs/zh_cn/开发相关.md`
- 本文件 `PLAN.md`
- `docs/hif/testing.md`
- `docs/hif/handoff.md`
- `assets/data/hif/pipeline_coverage.json`
- `assets/data/hif/roi_calibration.json`
- `assets/data/hif/screen_profiles/rinami_garakuta_road_20260709.json`
- 当前 `git status --short`、`git diff`、模拟器只读截图和最新 Journal。

若文档和当前画面冲突，以当前 `720x1280` ADB 原始帧、当前工作区及新生成的 Journal 为准；不得回退代码来迎合旧记录。

## Execution plan

### 0. 当前 Round1 五卡停点（优先级最高，覆盖第 1～5 节中过期的入口描述）

本节是下一次工作的唯一执行顺序。第 1～5 节的技能卡领取步骤、Round1 初始校准与旧单步入口均已完成或被替代，保留为审计历史，
不得从当前页面重复执行。

1. **建立不输入基线。** 先保存当前 MuMu 原始 `720x1280` 帧，确认仍为未选中 Round1 手牌页；审计最新 Journal、
   `git diff` 及工作区状态。页面、牌库数量或关键状态与上述停点不一致时，只做页面分类和证据保存，不执行出牌。
2. **先以测试固定新入口的契约。** 在 `tests/test_hif_live_runner.py` 与
   `tests/test_hif_action_safety.py` 增加最小用例，要求新入口必须同时满足：`--single-step`、初始无 `SELECT`、五张详情
   全部 IoU 映射、`missing_after=[]`、牌库读取成功、唯一非灰目标和执行后验。覆盖映射缺卡、标题/IoU 失败、灰牌、
   多目标、已有 `SELECT`、状态字段缺失、目标仍在手牌及未知后继页面，均应在危险输入前停止。
3. **实现显式同运行期单卡入口。** 在 `tools/hif_live_runner.py` 增加与普通
   `--round1-play-one` 互斥的入口（建议命名 `--round1-hand-detail-map-deck-play-one`）。它必须在一次运行中：
   读取五张详情并映射到初始 YOLO 框 → 读取牌库 → 重建完整 `ExamState` → 调用精确策略 → 仅在审批通过后进入
   两段式选中/确认。不得从外部参数注入卡名、坐标或历史状态。
4. **将唯一策略和动作前门禁保持可复算。** 仅在 `round1`、`good_condition >= 8`、候选名准确归一化为
   `話題沸騰`、该框非 `useless` 且唯一、全部必需数值已读、ROI 校准仍声明可执行时，
   `choose_high_good_condition_topic_card()` 才能返回该目标。任何卡名、详情、状态或校准冲突均返回安全停止，
   不回退为泛化“出好调卡”或普通 `--round1-play-one`。
5. **执行只允许一张卡的两段式动作。** 第一次点击后必须在同一目标框详情中同时验证精确标题和 `SELECT`；第二次
   点击只能作用于该已绑定目标。若首次帧已经含 `SELECT`、目标框变化、标题不符或控件不唯一，零确认输入后停止。
6. **以语义后验收口本次单步。** 执行后有限重读，必须同时证明：仍是已知 Round1 页面、`話題沸騰` 离开手牌、
   回合或可信状态字段发生机制一致变化，并且没有未声明控制器输入。动画导致即时手牌不可读时，只能按已有有限等待
   规则重读；等待结束仍无法证明则记录失败并停止。成功后另起零输入观察更新手牌与状态，不在同一命令内连打第二张。
7. **在任何实机调用前后运行门禁。** 先运行定向测试、`python tools/hif_pipeline_check.py`、相关编译/Ruff 和
   `git diff --check`；单步后审计 Journal、Maa 控制器日志和前后帧。只有该单步后验完整，才以当前屏幕重新制定
   下一张卡的计划；不得预先启用 Round 循环、Interval 或 Round2。

### 1. [历史完成] 恢复上下文并建立基线

1. 确认分支、HEAD、工作区差异和未跟踪文件；禁止 `git reset --hard`、`git checkout --`、批量格式化或清理。
2. 运行只读控制器探针，保存带新 `run-id`/时间戳的当前帧，确认连接、尺寸和页面。探针不得点击：

   ```powershell
   python tools/hif_controller_probe.py --adb 127.0.0.1:16416 --adb-path "D:\game\MuMuPlayer-12.0\nx_main\adb.exe" --save "debug\hif-runtime\resume-current.png"
   ```

3. 若不在预期技能卡领取页：先分类当前页面；若是标题页/通信错误，按授权恢复同一局；若是已知后续页面，从实际页面接续；若状态无法证明，停止。
4. 在任何新实机点击前运行：

   ```powershell
   python -m pytest -q tests -k hif
   python tools/hif_pipeline_check.py
   ```

5. 基线失败时只修复能证明与当前 HIF 改动或工作区状态有关的问题；不得借机处理无关模块。

### 2. [历史完成] 重新枚举技能卡并冻结唯一决策

1. 当前已知选中槽为 `left`。先运行“纯决策”探针，允许依次选择三个槽读取详情，但不重抽、不领取、不进入 Round：

   ```powershell
   python tools/hif_live_runner.py --adb 127.0.0.1:16416 --adb-path "D:\game\MuMuPlayer-12.0\nx_main\adb.exe" --single-step --skill-reward-decide --skill-reward-initial-slot left --seconds 45 --run-id <unique-run-id>
   ```

2. 审计本次 Journal、前后帧和 Maa 日志：输入只能是候选卡槽点击；不得出现 `受け取る`、`再抽選`、通用退出、Round 或未声明坐标。
3. 三张卡都必须取得可归一化名称、关键效果和置信度，并映射到当前
   `rinami_good_condition_safe` / 莉波好调路线的显式评分依据。
4. 若卡名或效果不在目录、OCR 不稳定、候选不唯一或评分并列：保留现场，先新增最小离线夹具/适配器结果和失败测试，再修改
   `catalog.py`、决策数据或识别逻辑。不得用截图观感直接决定领取。
5. 只有三槽枚举完整、唯一推荐可复算且当前所选槽可重新验证时，才进入领取阶段。
6. 免费重抽不是默认动作。仅当三张候选均被硬门拒绝、剩余次数 OCR 唯一确认、重抽按钮唯一定位，并已实现“次数减少 + 候选集合变化”的后验时，才可使用一次免费重抽；不得购买重抽。

### 3. [历史完成] 领取唯一技能卡并验证进入 Round1

1. 使用运行器的受限领取入口重新枚举并领取唯一推荐卡，不能将上一步结果作为不经复核的点击参数：

   ```powershell
   python tools/hif_live_runner.py --adb 127.0.0.1:16416 --adb-path "D:\game\MuMuPlayer-12.0\nx_main\adb.exe" --single-step --skill-reward-receive --skill-reward-initial-slot <current-selected-slot> --seconds 60 --run-id <unique-run-id>
   ```

2. 领取前必须重新验证提示/详情页、三候选、唯一推荐、当前选中卡名和唯一 `受け取る` 按钮。
3. 领取后只接受两种目标后验：
   - 直接命中已校准的 `Round1` 页面；
   - 命中同名技能卡展示层，随后用显式卡名运行
     `--skill-reward-reveal-confirm --skill-reward-reveal-name <name>`，且其点击后必须命中 `Round1`。
4. 动画帧变化、按钮消失或未知画面均不算成功；记录证据并安全停止，先补页面分类/后验再继续。

### 4. [历史完成] Round1 只读采证与执行就绪校准

在任何出牌前完成本阶段；不得简单把 `postcondition_supported` 改为 `true`。

1. 以观察模式运行 `ProduceCardsHIF`，记录原始帧、Maa OCR、YOLO 手牌检测、策略输出、置信度和
   `missing_fields`，确保零点击。
2. 校准 `assets/data/hif/roi_calibration.json` 中当前 MuMu 设备的 Round 数值 ROI，至少覆盖策略和后验真正需要的：
   回合/剩余回合、目标/当前参数、体力，以及好调、集中、元气等当前策略读取的状态。只填写实帧可重复读取的字段。
3. 验证手牌检测：每张可见卡必须有稳定名称、唯一 box、合理边界和去重结果；重叠牌或未知牌不得成为可执行目标。
4. 为首张计划出牌建立语义后验，至少同时满足：
   - 点击前唯一目标卡与策略动作一致；
   - 点击后重新识别 Round 页面，而非未知页/结算页误判；
   - 手牌集合或目标卡位置发生符合出牌语义的变化；
   - 回合/剩余回合或参数/状态中至少一个可信字段发生符合规则的变化；
   - 若牌具有一次性使用语义，运行期 session 正确记录且不会重复使用。
5. 将后验实现为独立、可离线测试的比较函数；为成功、帧变化但语义未变、目标页丢失、字段缺失、手牌不唯一和结算过渡分别写测试。
6. 只有校准文件声明执行就绪、状态读取无必需字段缺失、目标唯一、后验测试通过时，审批器才可收到
   `postcondition_supported=true`。该值必须来自真实能力检查，不得硬编码放行。

### 5. [由第 0 节替代] Round1 有界单步与自动闭环

1. 为 `tools/hif_live_runner.py` 增加明确的 Round 单步实验入口（名称以实现为准，例如
   `--round1-play-once`），必须与 `--single-step` 绑定，并仅覆盖 `ProduceHIFRound1ActionFlag`。
2. 首次只执行一张唯一目标牌。审计 Journal、控制器 Click、前后帧和后验字段；任何一项不一致立即停止。
3. 首张牌实机后验通过后，保留回归测试，再实现**有界**自动循环：每次循环都重新截图、完整读状态、重新决策、审批、点击一张牌、验证后验；不得缓存上一手坐标或盲连点。
4. 连续执行必须有最大动作数、单步超时、相同帧/相同状态检测、重复目标保护和未知页面退出；进入结算/Interval 即退出 Round 循环并交还正式根路由。
5. 当前进程中的 `HIFRunSession` 必须跨本 Round 各步保持，已使用关键卡状态不得在循环中丢失。若工具进程崩溃后无法从屏幕重建必要状态，停止而非假设。
6. Round 自然结束、失败或进入下一已知页面都必须由专用识别确认，不得依赖动作次数推断。

### 6. Interval 与 Round2

1. 先观察 Interval 页面并读取完整候选、P 点、体力和按钮；现有唯一已证明安全的默认动作是选择 `结束`，不购买、不刷新。
2. 若当前路线策略需要购买/强化/删除：先完成候选枚举、价格与余额 OCR、唯一决策、余额变化/商品状态变化后验，再按当前局资源授权执行；资料不足时选择已验证的 `结束`，不得猜购买。
3. Round2 重复 Round1 的“观察 → 校准 → 单张实测 → 后验 → 有界循环”流程。Round1 的 ROI/后验不能未经实帧验证直接视为 Round2 可用。
4. Round2 使用独立 round key、总回合数和 session 记录，验证不会串用 Round1 已出牌状态。

### 7. 后续日程、事件、奖励、Live、回忆与结算

1. 每次返回正式根路由后，优先匹配已知交互/结果页，再考虑安全空白推进；不得让通用节点抢占技能卡、变卡、咨询、Live、回忆或结算页面。
2. 已有单步动作继续遵守 `docs/hif/testing.md` 的安全后验；新出现页面先只读分类、保存最小证据、写失败测试，再增加识别或动作。
3. 资源候选一律执行“枚举全部 → 归一化 → 唯一决策 → 重选验证 → 提交 → 目标后验”。
4. Live 快进只有在明确目标页后验完成后才能点击；动画变化不算成功。
5. 回忆照片、生成、预览与结算链逐页验证；不得跳过中间状态或使用通用坐标推进。
6. 考试失败时仅使用已识别的免费重试/继续路径；遇到有偿确认、宝石提示或购买页面立即停止。
7. 最终完成必须命中已知结算完成/返回主页后验，并确认没有开启下一局。

### 8. 自动恢复与外部异常

1. 每次工具异常先只读截图并区分：游戏内未知页面、通信错误、游戏进程退出、ADB 断开或代码异常。
2. ADB 断开时仅重连既有 `127.0.0.1:16416` 实例；不得启动其他实例。
3. 通信错误或进程退出时允许重启游戏，进入标题页后只执行 `Tap to Start` 和当前局
   `プロデュース再開`；核对 HIF、本战日数和当前局信息后才确认恢复。
4. 不切换账号、不输入凭据、不删除数据、不清缓存。恢复入口不唯一或显示新开局时停止。
5. 恢复后必须重新识别当前页面和关键运行状态；不能直接续用崩溃前坐标、候选或未持久化 session。

### 9. 测试驱动与差异控制

1. 每个实测缺口先保留最小失败用例，再做最小实现；优先提取纯识别、解析、状态比较和后验函数，设备 I/O 留在 Action/runner 层。
2. 保持 Pipeline 节点名、Custom Action/Recognition 名、任务配置、preset 和数据结构向后兼容；新增实验权限默认关闭。
3. 新节点、跳转或测试场景必须同步 `assets/data/hif/pipeline_coverage.json`；新 ROI 必须是原始 `720x1280` 坐标并有来源。
4. 可版本化的真实帧仅限任务所需的最小、无账号标识帧，并登记来源和 SHA-256；默认先放 `debug/`，经过审阅才进入
   `tests/fixtures/hif/frames/`。
5. 不覆盖、移动、删除或格式化无关文件。提交前区分本轮差异与既有未跟踪内容。

## Verification gates

每个实机动作前运行与改动直接相关的定向测试；完成一个阶段后至少运行：

```powershell
python -m pytest -q tests -k hif
python tools/hif_pipeline_check.py
python -m py_compile agent/hif/*.py agent/hif/adapters/*.py agent/custom/action/produce_hif.py tools/hif_*.py
python -m ruff check agent/custom/action/produce_hif.py agent/hif tests/test_hif_*.py tools/hif_pipeline_check.py tools/hif_live_runner.py
npx prettier --check "assets/data/hif/pipeline_coverage.json" "assets/data/hif/roi_calibration.json" "assets/data/hif/screen_profiles/rinami_garakuta_road_20260709.json"
git diff --check
```

最终交付前运行全仓：

```powershell
python -m pytest
```

`npx maa-tools check` 仅在本地已有可用官方工具时执行；当前 registry 404 不属于项目校验失败，不得安装来源不明的替代包。

每个实机 run 必须有唯一 `run-id`，并审计：

- Journal 的决策、审批、输入坐标、前后证据和停止原因；
- Maa 日志中的所有 Click/Swipe，确认没有未声明输入；
- 原始帧尺寸与页面分类；
- 资源提交后的语义后验；
- 是否触发安全停止及其原因是否与画面一致。

## Completion criteria

只有同时满足以下条件才标记本计划完成：

- 当前这一局 HIF 已从技能卡领取页安全推进到最终结算，并确认没有开启下一局；
- 技能卡领取、Round1、Interval、Round2 以及实际经过的后续页面均有可审计前置、输入与后验；
- Round 每次出牌都由完整状态、唯一目标和语义后验批准，没有盲点或仅凭帧变化的成功记录；
- 新发现缺口都有失败测试和最小修复，HIF 专项、结构检查、相关编译/Ruff/格式检查及全仓 pytest 通过；
- `debug/`、日志、Journal、视频、压缩包和无关资源没有进入提交差异；
- `docs/hif/testing.md` 与 `docs/hif/handoff.md` 已更新为本次真实证据、仍保留限制及最终状态；
- 最终 `git diff` 已人工式审阅；未经用户明确要求不 stage、commit 或 push。

## Hard stop conditions

遇到以下任一情况，保存当前帧、Journal、日志和复现信息后停止，并向用户报告，而不是扩大授权：

- 页面、按钮、候选或目标不唯一，且无法通过只读采证与代码修复证明；
- 需要有偿宝石、现实货币、购买、账号操作、切换账号或输入凭据；
- 只能通过放弃当前局、删除数据、清缓存或开启下一局继续；
- ADB 指向不同实例/不同设备，或恢复页面不能证明属于当前 HIF 局；
- 点击后无法建立语义后验，或游戏状态与 Journal/策略结论冲突；
- 当前局已因外部操作发生不可恢复变化，继续将依赖猜测；
- 测试暴露与本计划无关的高风险仓库问题，修复会显著扩大范围。

## Key tradeoffs

- 用户以当前局完整授权换取最少人工介入，但所有安全后验仍保持；“全自动”不等于“无条件连点”。
- 为保护当前唯一实机现场，优先离线测试和单步验证；连续模式只能从已实证的单步后验逐步放开。
- 允许在本局内消耗普通资源和免费恢复机会，以完成真实闭环；明确排除付费和账户级风险。
- 完整跑通当前 `rinami_garakuta_road` 是本轮完成线，不借机声称其他偶像、路线或下一局已经受支持。

## HIF 出牌决策算法冻结规格

_Locked via grilling — 由用户与当前 Codex 代理于 2026-07-15 逐项确认_

本节总结 `rinami_garakuta_road` 的 HIF Round1/Round2 出牌决策算法。它定义后续算法工程与上线门槛，
不扩大前述当前实机授权，不允许绕过完整状态、唯一目标、两段式确认、语义后验或其他硬停止条件。

### 1. 范围与优化目标

- 首版只覆盖姫崎莉波 `ガラクタロード` 的 HIF Round1/Round2，不宣称支持其他偶像、流派或场景。
- 优化目标按词典序排列：
  1. 排除非法、灰牌、状态不完整、模型不支持和可能破坏关键循环的动作；
  2. 优先保证 HIF 优胜；
  3. 达到安全优胜线后，最大化 `1.2 × Round1 分数 + Round2 分数` 的期望值。
- 使用最差 10% 情景的平均加权总分 `CVaR₁₀` 衡量坏牌序风险，不以平均分单独决定动作。
- 动态目标线定义为：

  ```text
  基础目标 = max(可信实时对手目标, 当前配置档历史 P95 优胜线)
  安全目标 = 基础目标 + max(50,000, 基础目标 × 5%)
  ```

- 实时目标不可读时回退到历史 P95；历史样本不足时可暂用攻略经验值冷启动，但必须标记低置信度，
  不得将固定分数写成永久真值。
- 存在 `CVaR₁₀ >= 安全目标` 的动作时，只在这些安全动作中最大化期望总分；不存在时选择
  `CVaR₁₀` 最高的动作，优先降低失败损失。

### 2. 状态模型与真值来源

- 使用纯 Python、白名单式高保真牌局模拟器；TypeScript/Node 引擎只可作为离线参考或差分对照，
  不成为生产运行时依赖。
- 规划器只接受唯一、已验证、可规范化的 `BattleState`。至少包含：
  - Round、当前/剩余回合、当前审查属性与倍率；
  - 当前分数、体力、集中、好调及所有已支持持续状态；
  - 完整手牌及强化等级、牌库/弃牌堆/除外区的卡牌多重集合；
  - 卡牌使用次数、再演次数、关键卡已使用标记；
  - P 道具、P 饮料、额外抽牌/换手能力和跨 Round 资源。
- “下一张牌未知”按已知剩余牌组的无放回随机事件建模；剩余牌组组成不可信时不得启用搜索。
- 首版不维护多个 OCR 假设，不实现 belief state、POMDP 或 POMCP。识别冲突必须有限重读，仍不唯一则停止。
- 真值优先级：
  1. 当前游戏实机的动作前后状态轨迹；
  2. 解包得到的结构化 Master 数据；
  3. `gakumas-core` 等公开模拟器；
  4. Wiki、攻略与经验规则。
- 来源冲突时以实机为准；无法解释的卡牌、强化等级或效果必须移出白名单，不能猜测实现。

### 3. 状态转移与动作空间

- 为当前路线可能出现的卡牌、强化等级、P 道具和 P 饮料逐项实现显式状态转移；不要求首版覆盖全卡池。
- 合法动作统一包含：
  - 使用一张具体且可执行的卡牌；
  - 使用一个已支持的 P 饮料；
  - 已验证的额外抽牌；
  - 已验证的换手；
  - 结束当前行动。
- 不消耗正常出牌次数的资源动作使用独立动作阶段；每个消耗品最多使用一次，每个动作必须改变规范化状态，
  防止搜索产生零进展循环。
- 灰牌、条件不足、资源不足、重复使用和不支持效果由 Hard Gate 排除，搜索器无权覆盖这些规则。
- 状态完整但搜索超时、候选无法可靠区分或局部模型不支持时，只能回退到能产生唯一、显式、已验证目标的
  `GarakutaRinamiStrategy` 规则；状态不完整时不得使用启发式猜测。

### 4. 在线规划算法

- 核心算法冻结为“情景采样 Sparse Expectimax + 滚动时域重规划（MPC）”。
- 每次只执行当前最优动作；完成截图、状态重读和语义后验后，从真实新状态重新规划。
- 所有根动作使用同一随机数流生成未来牌序，避免候选因面对不同抽牌运气而产生比较偏差。
- 对关键卡早抽、晚抽、未抽到等低概率高影响事件进行分层采样，并按真实概率加权。
- 搜索调度：
  1. 128 个共同情景用于初步比较；
  2. 256 个共同情景作为基础完整比较；
  3. 候选接近、使用稀缺资源、接近终结窗口或优胜线时，按 64 个情景一批追加到 512～1024；
  4. 随机后继足够少时自动切换为精确枚举。
- 标准动作深度为 2～3；预算充足且前两名候选仍接近时，只对前两名选择性搜索深度 4。
- 只采用完整完成的深度和样本批次；不能把超时中断的半批结果混入最终统计。
- 12 秒软限制之后不再开启新深度或样本批次；16 秒硬限制必须返回最后一个完整批次的结果。
- 候选已可靠分离时允许提前返回，不得为了用满预算继续无收益计算。
- 性能上线门槛：决策耗时中位数不超过 4 秒，P95 不超过 12 秒，绝对最大值不超过 16 秒。

### 5. 叶节点估值与跨 Round 续局价值

- 已结束局面使用真实分数、动态优胜目标与最终优胜结果，不再调用近似估值。
- 未结束局面的可解释特征至少包括：
  - 当前分数、剩余回合、审查属性倍率和预计优胜缺口；
  - 集中、好调及其他持续状态在剩余回合中的未来收益；
  - 再演次数、关键卡位置、循环完整度和终结技可达概率；
  - 体力安全余量、P 饮料、额外抽牌/换手资源；
  - 手牌、牌库、弃牌堆、除外区和关键卡未来抽取概率。
- Round1 不搜索完整 21 回合，而在叶节点加入 Interval/Round2 续局价值；Round1 结束后读取真实分数、资源和牌组，
  重新规划 Interval 与 Round2。
- 不硬编码“必须留一瓶饮料”或“Round1 必须用光资源”；资源使用由总成绩的 `CVaR₁₀` 与续局价值决定。
- 特征由人设计并保持可解释；权重通过离线高预算搜索/完整模拟结果进行受约束校准，不训练神经价值网络。
- 权重训练牌序与验收牌序必须分离；每个特征、权重、分项值和最终值都写入可复算日志。

### 6. 可复现性与审计

- 随机种子由规范化状态哈希与算法版本生成，不使用当前时间。
- 每次决策日志至少保存：
  - 输入状态、状态来源和置信度；
  - 算法/卡牌数据/估值权重版本；
  - 随机种子、采样分层、样本数、完成深度和耗时；
  - 每个候选的均值、中位数、P10、`CVaR₁₀`、失败比例和目标线差值；
  - 最终动作、提前停止/回退原因以及点击前后验。
- 同一输入状态、算法版本和数据版本必须得到相同决策，允许离线逐状态复算。

### 7. 实施阶段

1. 定义不可变、可哈希的 `BattleState`、动作、状态差分和不变量；扩展 Session 以保存已验证的牌组/弃牌/除外信息。
2. 建立当前路线白名单和效果解释器，为普通得分、集中、好调、体力、抽牌、换手、除外、再演、使用次数追加、
   P 道具和 P 饮料补单元测试。
3. 实现合法动作生成、Hard Gate、确定性状态转移和终局分数计算。
4. 实现分解式叶节点估值、Round2 续局价值和配置化权重。
5. 实现共同随机数、分层采样、Sparse Expectimax、MPC、完整批次超时和状态缓存。
6. 将规划器接入现有观察/审批/执行链；保持影子模式和单步权限默认值，不直接开放连续模式。
7. 使用高预算离线结果校准估值权重，并在独立牌序上与当前启发式、深度 1 贪心和离线高预算基准比较。
8. 按“影子预测 → 单步实机 → 有界连续执行”逐级验证，不跨级开放权限。

建议目录边界：

```text
agent/hif/battle/state.py       # 规范化牌局状态
agent/hif/battle/effects.py     # 白名单效果与状态转移
agent/hif/battle/actions.py     # 合法动作生成与 Hard Gate
agent/hif/battle/evaluator.py   # 叶节点与续局价值
agent/hif/battle/planner.py     # 情景采样 Sparse Expectimax
agent/hif/battle/trace.py       # 可复算决策日志
```

### 8. 验证与上线门槛

- 每张白名单卡及其强化等级都有状态转移测试；每种特殊机制至少有一条实机黄金轨迹。
- 影子模式至少完整覆盖一轮 Round1 和一轮 Round2，所有已支持字段均无未解释预测偏差。
- 实机轨迹出现一次未解释偏差时立即停止并隔离对应效果；增加搜索预算不能替代模型修复。
- 使用未参与调权的独立牌序测试集，与现有启发式进行共同牌序、成对比较；测试持续到优胜率误差约为 ±1%。
- 模拟优胜率的保守估计必须达到 95%，`CVaR₁₀` 不低于现有启发式，平均加权总分应显著提高。
- 相对离线高预算基准，绝大多数局面的最终得分损失控制在 5% 以内。
- 所有测试中的非法动作、灰牌动作和不支持效果执行次数必须为 0。
- 满足模型、效果、统计、性能和实机后验全部门槛后，才允许连续模式替换当前正式策略。

### 9. 互联网依据

- [CEDEC 2024：学マス的 MDP、MCTS、PPO 与迁移学习实践](https://www.famitsu.com/article/202408/14977)
- [公开的学マス深度 3 滚动前瞻实现](https://github.com/katabami83/gakumas_contest_simulator)
- [公开学マス卡牌核心模拟引擎](https://github.com/kjirou/gakumas-core)
- [Berkeley CS188：Expectimax](https://inst.eecs.berkeley.edu/~cs188/textbook/games/expectimax.html)
- [Berkeley CS188：MCTS](https://inst.eecs.berkeley.edu/~cs188/textbook/games/monte-carlo.html)
- [HIF 两轮计分、Interval 与资源关系](https://seesaawiki.jp/gakumasu/d/H.I.F)
