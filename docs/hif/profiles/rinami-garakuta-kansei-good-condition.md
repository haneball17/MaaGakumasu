# `姫崎 莉波（ガラクタロード）` HIF `感性·好调` 路线资料

本页只负责当前主路线的角色资料，不承担 HIF 通用机制说明。

## 1. 当前角色路线定位

当前主目标固定为：

- 角色卡：`姫崎 莉波（ガラクタロード）`
- 剧本：`HIF`
- 路线：`感性·好调`

## 2. 当前路线判断

当前文档统一采用下面的开发抽象：

- `plan = センス`
- `build = 好調`

当前主副属性倾向仍按：

- 主属性：`Vi`
- 副属性：`Da`

## 3. 当前路线的重要变量

从现有资料看，这条路线特别需要关注：

- 星性
- 记忆质量
- `好调` 相关核心技能
- 卡组厚度
- `Interval` P 点预算
- P 道具协同

## 4. 当前对 simulator 的输入意义

这条路线当前主要映射到：

- `ProduceProfile`
- `preferred_skill_tags`
- `preferred_p_item_tags`
- `reward_priorities`

## 5. 当前仍未固化的部分

还没有完全固定下来的内容包括：

- 支援卡完整 6 卡编成
- 奖励池中的最优拿卡顺序
- 高评价阶段的精细 P 道具排序
- 实机下的完整时点策略

## 6. 一句话总结

这条路线当前更适合作为：

> CLI 模拟和后续 HIF 自动化开发的第一条基准路线。
